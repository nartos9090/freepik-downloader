import { getSavedCookie, setCookie } from './cookie/cookie'
import { downloadByUrl as downloadByUrlV2 } from './downloader/auto-downloader'

export default {
  setCookie,
  getSavedCookie,
  downloadByUrlV2
}
